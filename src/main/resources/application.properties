# Server Configuration
server.port=${PORT:8080}

# Database Configuration
# Production: Uses DATABASE_URL from Render
# Local: Uses local PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/riskmonitor}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:aryan29jun}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:riskmonitor-secret-key-must-be-long-enough-2024-barclays}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration - Allow your Vercel frontend
spring.web.cors.allowed-origins=${ALLOWED_ORIGINS:https://financial-risk-analyzer-barclay-degiwn5ek.vercel.app,http://localhost:3000}